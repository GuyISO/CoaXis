# Viewer

## 3D表示関係
点群座標へのVFX配置
    GodotのVFX Graphを使用して、点群座標にエフェクトを配置する機能を実装する
3Dから2Dに投影してテキストを表示
    3D空間の特定の位置にテキストを表示するために、カメラ投影を使用して2Dスクリーン座標に変換し、UI上にテキストを配置する機能を実装する
レイヤー管理
    表示非表示以外のレイヤーを用意、分類は未決定
        Model、VFXでわけるか？
    透明度をレイヤー別で設定可能にする？

コマンド化、IPC化進める

イベントの経路やFacadeを見直す
    イベントを経由せずサービス層を直接参照可能な処理はそうする
ModelServiceの責務がおかしいので、ModelServiceの責務を整理する
    ModelServiceはModelの読み込み・保存・削除などの処理を行う
    Modelの表示状態やVFXの切り替えなどはModelServiceではなく、別のサービスで行うようにする
    ModelServiceはModelの状態を管理するだけにする
ModelTree
    ModelAddedで都度追加するのやめて、一斉に描画更新

## UI
設定画面
    設定変更したら、Settings.jsonに保存する
各種パネル
    フローティングor右側でタブから選んで使用できるように配置する
Treeの切り出し
    Treeを独立したUIコンポーネントとして切り出し、他のUIからも再利用可能にする
プラン未承認のものが表示されている場合のNotAuthorizedの透かし

## 細かい修正

## 先延ばしにしたやつとか
Dockable Window
    各UIをDock可能なContainerの入れ子として使えるようにする
点群座標をカメラ投影しての点配置
    とりあえず3Dモデルで始めるのでまだ先
断面表示
    需要があれば実装する、今のところは必要ない
干渉・クリアランスチェック
    需要があれば実装する、今のところは必要ない
Modelの移動機能
    選択したモデルを移動できるようにする、移動可否はModelの属性で制御する
    ワールド移動、ローカル移動、Snapping機能は欲しい
選択セット・表示セットの保存・復元
    選択セット・表示状態、ツリーの展開状態などのセットを保存して、後で復元する機能、しばらくは需要がなさそう
    
## 既知のバグ
レイキャスト
    glbからロードしたモデルがレイキャストを受け付けない場合がある
    LineSetも角度によってはレイキャストを受け付けない場合がある
範囲選択
    至近距離になるとPickByShapeが効かなくなる場合がある